<%= form_with(model: visit) do |form| %>
  <% if visit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(visit.errors.count, "error") %> prohibited this visit from being saved:</h2>

      <ul>
        <% visit.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :visitor_who %>
    <%= form.text_field :visitor_who %>
  </div>

  <div class="field">
    <%= form.label :visit_to %>
    <%= form.text_field :visit_to %>
  </div>

  <div class="field">
    <%= form.label :problem %>
    <%= form.text_field :problem %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
